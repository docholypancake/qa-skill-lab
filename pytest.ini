[pytest]
# Root config — used only when running from repo root (e.g. smoke suite).
# Module-level pytest.ini files are used for per-module runs.
testpaths =
    03-python-automation/tests
    04-e2e-playwright/tests
pythonpath =
    04-e2e-playwright
markers =
    smoke: critical-path tests that verify the system is alive
