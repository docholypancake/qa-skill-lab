[pytest]
testpaths = tests
# Add the project root to sys.path so `from pages.home_page import HomePage` resolves
pythonpath = .
markers =
    smoke: critical-path tests that verify the system is alive
